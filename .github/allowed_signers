# Keys permitted to sign commits in this repository.
#
# Checked by tools/verify_commits.py, which is the pre-push gate. A commit
# signed by a key that is not listed here fails verification even though the
# signature itself is cryptographically valid -- an unpinned key is an unknown
# signer, and "the signature verified" is not the same claim as "the right
# person signed it".
#
# To add a maintainer: append their public key, and have an existing maintainer
# sign the commit that adds it.
noreply@anthropic.com namespaces="git" ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKy87HxSEheG8vEPhSs9u2KZCtVErAQfpmprtUJCZ2w7
